# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'TaterTotTimer' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TaterTotTimer
  pod 'RxSwift'
  pod 'RxCocoa'

  target 'TaterTotTimerTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TaterTotTimer WatchKit App' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TaterTotTimer WatchKit App

end

target 'TaterTotTimer WatchKit Extension' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TaterTotTimer WatchKit Extension

end
